\begin{table}
  \caption{\label{tab:sim-params}The parameters used to simulate the data set.}
  \centering
  \fbox{
    \begin{tabular}{lc}
      Parameter & Value \\
      \hline
      Birth rate & {{birth_rate}} \\
      Death rate & {{death_rate}} \\
      Sampling rate & {{sampling_rate}} \\
      Catastrophes probability & {{catastrophe_prob}}
      Catastrophes times & \{ {{catastrophe_times}} \} \\
      Occurrence rate & {{occurrence_rate}} \\
      Disasters probability & {{disaster_prob}}
      Disasters & \{ {{disaster_times}} \} \\
      Duration & {{simulation_duration}} \\
    \end{tabular}
  }
\end{table}